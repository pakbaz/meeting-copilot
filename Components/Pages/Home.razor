@page "/"
@inject NavigationManager Navigation

<PageTitle>Meeting Copilot</PageTitle>

@code {
    protected override void OnInitialized()
    {
        // Redirect to pre-meeting setup as the main entry point
        Navigation.NavigateTo("/meeting/setup", replace: true);
    }
}
